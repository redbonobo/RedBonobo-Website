import React from "react";
import ReactDOM from "react-dom";

const element = <h1>hello World</h1>; //babel will compile this into javascript
ReactDOM.render(element, document.getElementById("root"));
